<UserControl x:Class="PortAbuse2.Controls.BlockInOutMenu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PortAbuse2.Controls"
             DataContext="{Binding RelativeSource={RelativeSource Self}}"
             mc:Ignorable="d" d:DesignWidth="300">
    <Grid Margin="-30,-5">
            <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <MenuItem Header="{Binding ItemText, FallbackValue='- sec'}" Click="BlockClicked" Margin="-1,-6" VerticalContentAlignment="Center" VerticalAlignment="Center"/>
        <MenuItem Grid.Column="1" MinWidth="30" Header="I" Click="Button_Click" Margin="-1,-6" VerticalAlignment="Center" VerticalContentAlignment="Center"/>
        <MenuItem Grid.Column="2" MinWidth="30" Header="O" Click="Button_Click_1" Margin="-1,-6" VerticalAlignment="Center"/>
    </Grid>
</UserControl>
